langcode: en
status: true
dependencies:
  config:
    - user.role.project_admin
  module:
    - user
id: user_remove_role_action.project_admin
label: 'Remove the Project Admin role from the selected user(s)'
type: user
plugin: user_remove_role_action
configuration:
  rid: project_admin
